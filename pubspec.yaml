name: minim
description: Minimal android launcher written in flutter.
version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  launcher_assist: ^1.1.3
  flutter_appavailability: ^0.0.21

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
